## Skills Directory Configuration

You are configured to create skills in the **user global** directory at `~/.pi/agent/skills/` instead of the project-local directory. This makes skills available across all projects for this user.

When creating or managing skills, use the global user path: `~/.pi/agent/skills/[skill-name]/SKILL.md`